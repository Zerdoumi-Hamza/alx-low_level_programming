#include "main.h"

/**
*_abs  - a function that prints the absolute of an integer. 
*@n: the number need to cheeck it.
*
*Return: 1 if > 0 0 if 0 and -1 if < 0.
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else (n < 0)
		return (-n);
}
