#include "main.h"

/**
*print_array - Function that prints n elements of an array of integers.
*@a: recieve the array should be printed.
*@n: recieve the nuber of elements of the array to be printed*Return: Nothing.
*/

void print_array(int *a, int n)
{
	for (i = 0; i <= n; i++)
	{
		printf("%d,", *(s + i));
		printf(" ");
	}
	printf("\n");
}
